# 育见 ChildOS 测评清单

> 与 `优化清单.md` 配套：优化清单记录**做了什么**，本清单记录**怎么验证它真的成立**。
> 每条给出「测评目标 / 操作步骤 / 预期结果 / 状态」，可照着逐条跑。

## 维护协议（重要）

**每完成一项新功能或修复，必须同时更新两份文档：**
1. `优化清单.md` —— 在对应项打勾、记 commit；
2. `测评清单.md`（本文件）—— 新增/更新该项的测评条目（操作步骤 + 预期 + 状态）。

状态图例：`✅ 已验证`（已实测通过）｜`☐ 待验证`（依赖未配置或未跑）｜`⚠️ 部分`（仅代码验证/未运行时测）。

---

## 0. 测评环境准备

| 档位 | 配置 | 能测什么 |
|---|---|---|
| **A. 最小**（无 key 无 DB） | 默认 `.env`，`NEXT_PUBLIC_USE_MOCK` 默认 | 类型/构建门、降级文案、前台字段收敛、追问结构 |
| **B. 带 LLM key** | `FAST_AI_API_KEY` / `EMBEDDING_API_KEY`（阿里百炼） | 真流式回复、Episode 抽取、向量精排、画像感知预演 |
| **C. 全量（Docker pgvector）** | B + 起 pgvector + `DATABASE_URL` + `USE_MOCK=false` | 三层语义检索、job_queue 状态机、多租户隔离、Brief/Board 闭环、traceId 全链路 |

**起 Docker pgvector（档位 C）：**
```bash
docker run -d --name childos-pgvector -e POSTGRES_PASSWORD=childos -e POSTGRES_DB=childos \
  -p 5433:5432 pgvector/pgvector:pg16
# .env.local:
#   DATABASE_URL=postgres://postgres:childos@localhost:5433/childos
#   NEXT_PUBLIC_USE_MOCK=false
#   FAST_AI_API_KEY=...  EMBEDDING_API_KEY=...   (阿里百炼，⚠️ 用后即弃/轮换)
npm run dev
# 测完：docker rm -f childos-pgvector
```

> ⚠️ **C 档构建坑（实测）**：`NEXT_PUBLIC_USE_MOCK` 是**构建期内联**变量。若用 `npm run build` + `node server.js`(prod) 跑 C 档，**必须在构建时就带 `NEXT_PUBLIC_USE_MOCK=false`**（写进 .env.local 再 build，或构建命令前置该 env），否则 prod 运行时 DB 启用会导致**全路由 500**。`npm run dev` 现编译、无此问题（dev 连真库验 DB 持久化完全有效）。

> ⚠️ key 只放环境变量/`.env.local`（已 gitignore），**禁止写进任何提交文件**；测试用 key 用完即轮换。

---

## 1. 通用验收门（任何改动后必跑 · 档位 A 即可）

| # | 测评项 | 命令 | 预期 | 状态 |
|---|---|---|---|---|
| G1 | 类型检查 | `npm run typecheck` | 0 error（含 prebuild 自动生成 prompt 注册表） | ✅ |
| G2 | 生产构建 | `npm run build` | 构建成功，无 standalone 缺文件 | ✅ |
| G3 | Prompt 构建 | `npm run prompts` | 由 `prompts/**/*.md` 生成 19 个 prompt 到 `registry.generated.ts` | ✅ |

---

## 2. 逐项功能测评（对应优化清单编号）

### P0-1 前台输出收敛为 visibleReply + traceId
- **目标**：`/api/daily` 不泄露 `decomposedInput`/`routingDecision`/`memoryAction`/置信度等内部字段。
- **步骤**：POST `/api/daily`（带登录态），观察返回 JSON 顶层字段。
- **预期**：仅 `{ ok, data: { traceId, visibleReply, linkedAreas } }`；无任何内部推理字段、无 confidence。
- **状态**：✅（build 验证 + 字段对齐 observation 页 `visibleReply`）

### P0-2 后台记忆写入异步化
- **目标**：用户看到回复不被记忆写入阻塞。
- **步骤**：发一条 daily，记录首字节/响应到达时刻；后台写入失败时观察前台是否受影响。
- **预期**：回复先到，写入在后台；写入失败仅日志，不影响前台返回。
- **状态**：✅

### P0-3 日常对话前台流式输出
- **目标**：`/api/daily/stream` NDJSON 流式；无 key 降级为规则文案不退化；风险输入走安全文案。
- **步骤**：①档位 B 发一句普通家庭困扰 → 看是否逐行流式；②档位 A（无 key）发同样输入 → 看是否规则文案且不空；③发自伤/危机类输入 → 看是否安全文案。
- **预期**：①真流式逐行 `final`；②降级文案完整可用；③安全文案优先。
- **状态**：✅（A 档降级路径已验；B 档真流式需带 key 环境复测）⚠️ 真流式

### 功能-1 家庭综合规划 family_planner
- **目标**：输出 1-3 个可承受动作；先稳边界；失败原因不清先追问（文档 5.4.3）。
- **步骤**：POST `/api/family-planner`：
  - 清晰失败节点输入：「孩子作业拖到半夜，之前定了晚饭后开始但我加班没人盯就崩了」
  - 模糊输入：「孩子学习不行，帮我规划下」
- **预期**：清晰输入 → `enoughToPlan=true` + 1-3 个 actions + `boundaryFirst` 非空；模糊输入 → `enoughToPlan=false` + `actions=[]` + `missingInfo` 追问失败节点。
- **状态**：✅（docker 实测：清晰→2 动作+边界；模糊→0 动作+追问）

### 功能-2 家庭支持看板 board
- **目标**：五段式看板由后台生成、读持久化快照，不前端伪造；信息不足温和空态。
- **步骤**：档位 C 走完几轮 daily 后 GET `/api/board`（带登录态）。
- **预期**：返回当前状态/稳定理解/互动模式/待验证点/下一步五段 + `version`/`evidenceRefs`/`updatedAt`；空数据时不硬编画像。
- **状态**：✅（见闭环-1 docker 验证）

### 架构-1 三层语义检索（EvidenceEpisode + FactAtom + pgvector）
- **目标**：语义完整 Episode 召回（非碎片）；FactAtom 标 sourceType；选择性向量化；跨用词语义召回。
- **步骤**：
  - 档位 B：`EMBEDDING_API_KEY=... node scripts/test-vector-retrieval.mjs`（向量精排 vs 取最近）
  - 档位 C：`DATABASE_URL=... EMBEDDING_API_KEY=... node scripts/verify-pgvector.mjs ["逃避"]`
  - psql 核查：`evidence_episodes` 有行带 embedding；`fact_atoms` 标 source_type；高价值 Atom embedding 非 NULL、普通 Atom embedding=NULL。
- **预期**：向量精排 top5 平均相关度显著高于取最近（实测 0.61 vs 0.48，+27%）；query「逃避」召回「背不会/摔本子」场景（跨用词）。
- **状态**：✅（docker 端到端通过）

### 架构-2 Prompt 外置（md 源 + 构建期生成）
- **目标**：19 个 prompt 由 `prompts/{front,background}/*.md` 编译进 bundle，零运行时 fs。
- **步骤**：改任一 `prompts/**/*.md` → `npm run prompts` → 看 `registry.generated.ts` 是否重生成；grep 生成文件无乱码（U+FFFD）。
- **预期**：18 个 prompt 全量生成，关键硬规则（dailyDialogue 的 Q2 追问、episodeExtractor 来源底线、familyPlanner 1-3 动作）保留。
- **状态**：✅

### 架构-3 traceId 全链路字段闭环
- **目标**：一条家长消息的 traceId 贯穿 job_queue / daily_updates / evidence_episodes。
- **步骤**：档位 C 发一条 daily 拿到 `traceId`，psql：
  ```sql
  SELECT job_type, trace_id FROM job_queue WHERE trace_id='<traceId>';
  SELECT data->>'sourceEventId' FROM memory_layer_items WHERE layer_name='daily_updates';
  SELECT source_event_id FROM evidence_episodes ORDER BY created_at DESC LIMIT 1;
  ```
- **预期**：两个 job 共享同一 trace_id；daily_update.sourceEventId == traceId；episode.source_event_id == 同一 traceId。
- **状态**：✅（docker 三方对齐）

### 体验-1 对话主入口 + 追问结构化
- **目标**：首页提交进入 `/daily` 多轮对话；关键追问显式说明「在区分什么」（文档 4.4）。
- **步骤**：档位 B 在 `/daily` 发一句信息不全的困扰 → 看 AI 追问文案开头。
- **预期**：追问以「这里想区分 X 还是 Y」结构出现。
- **状态**：✅（docker 实测 LLM 回复以"这里想区分…"开头）

### 闭环-1 Brief/Board 后台闭环 + 3 个前台读写缺口
- **目标**：① digest 链（memory_write→digest_update）；② content_hash 幂等不增版本；③ 预演 ContextPack profile-aware；④ 家庭规划读写记忆。
- **步骤**（档位 C）：
  - ① 发 daily → psql 查 job_queue 链路全 succeeded、`board_snapshots` version=1 有真实内容；
  - ② 同内容再发 → version 不 +1（hash 命中 DO NOTHING）；
  - ③ 预演页填目标/担心/背景 → `/api/rehearsal/analyze` 返回 `profileAware=true` 且 saferVersion 引用真实家庭历史；
  - ④ `/api/family-planner` 后 psql 查到 `[家庭规划]` 的 daily_update。
- **预期**：四点全部成立。
- **状态**：✅

### 审计-1 文档符合度审计修复（4 项）
- **目标**：知识库 schema 预留 / matchedMechanisms 不再污染 / 规划失败门槛 / 预演原话门槛。
- **步骤 & 预期**：
  - 知识库：daily/stream 输入有 `knowledgeContext` 占位且 P0 恒空，不接逻辑；
  - matchedMechanisms：psql 查 `daily_updates.matchedMechanisms`（不再被证据文本污染，机制为空时=`[]`）；
  - 规划门槛：见 功能-1；
  - 预演门槛：`/api/rehearsal/analyze` 空 parentText → 返回 `NEED_PARENT_WORDS`（非 BAD_REQUEST）。
- **状态**：✅（docker 验证）

### 架构-4 后台任务队列与重试 job_queue
- **目标**：五状态机 + 幂等 + 重试退避 + 僵尸回收 + 并发去重。
- **步骤**（档位 C，参考 8 场景）：
  - 同文本发 3 次 → psql 查 `evidence_episodes` **仅 1 行**（idempotency_key 去重）；
  - 制造失败 job → 看 `retrying`（带 last_error + 退避）→ 耗尽 max_attempts → `failed`；
  - 10 并发同文本 → 0 重复入队 → 1 Episode；
  - 人为置 stale running → 僵尸回收 → retrying → 重跑 succeeded 且仍 1 Episode。
- **预期**：上述全部成立；atom 无重复/孤儿。
- **状态**：✅（8 场景 docker 通过）｜⚠️ DB 未启用的 inline 降级路径为代码验证、未运行时测

### 质量-1 多租户隔离
- **目标**：记忆域按 family_id/child_id 隔离；A 检索看不到 B；PK 不跨租户覆盖。
- **步骤**（档位 C）：注册两个真实用户 A/B，各发 daily：
  - psql 按 `family_id` 分组查 `evidence_episodes`/`fact_atoms`/`memory_layer_items`；
  - 用 A 的检索域确认查不到 B 的事实；
  - 两租户同名 `learning_homework` entry pack 共存验证。
- **预期**：完全隔离；同名 entry pack 不互相覆盖。
- **状态**：✅（docker 双用户隔离通过）

### 质量-2 内存降级显式告警
- **目标**：首次降级到进程内存时 `console.warn` 一次，不静默。
- **步骤**：档位 A（无 DATABASE_URL）启动并触发一次记忆读写。
- **预期**：日志出现一次「数据未持久化、重启丢失」告警。
- **状态**：✅

### 质量-3 过时测试脚本归档
- **目标**：过时脚本归档保留 git 历史，活跃脚本仍可用。
- **步骤**：`ls scripts/legacy/`（应有 12 个 + README）；`ls scripts/*.mjs`（应仅剩 build-prompts / verify-pgvector / test-vector-retrieval / test-input-strategy / test-profile-rehearsal）。
- **预期**：归档与活跃区分清晰。
- **状态**：✅

### 质量-4 diagnosis / synthesis 写入异步化
- **目标**：画像生成管线写入异步、返回更快；前端仍能拿到组装画像所需完整字段。
- **步骤**：档位 B/C 走 `/profile/generating` 全流程。
- **预期**：画像正常生成；写入不阻塞返回；前端 `candidateMechanismMatrix` 等字段可用。
- **状态**：✅（含「不应收敛 diagnosis/synthesis 输出」的误判修正）

### 功能-3 教育模式诊断独立入口 + 5.3 UI 切换基座
- **目标**：教育诊断作为第三并列入口，按 readiness 在 专项采集 / 轻追问 / 结果展示 三态切换；信息不足只追问不硬生成；前台不暴露后台字段。
- **步骤 & 预期**：
  - **A 档（无 key）**：`/api/education-diagnosis`（带 demo cookie）输入笼统一句 → 返回 `uiMode=special_collection` + 降级 `collectionGuide` + `result=null`；grep 响应断言**无** `readiness/coverageScore/confidence/missingHighImpactFacts` 字段名（红线）。
  - **C 档（带 DashScope key）**：
    - 输入笼统「我们家教育是不是有问题」→ `uiMode=special_collection`（引导多讲）；
    - 输入**残缺**生活流水（只说上学日、缺周末/自主时间）→ `uiMode=light_followup`，`followupPrompt` 指向缺的要素（如"周末有没有一段真正属于孩子自己的时间"），**不硬生成**；
    - 补齐周末/谁管学习/自主时间 → `uiMode=result_view`，`result.modeReading` + `keyTensions`（1-3）；
    - psql：`[教育诊断]` dailyUpdate 已写、`evidence_episodes` 有对应 Episode。
  - **首页**：可见三并列「专项功能」卡 + 底栏教育诊断入口可跳 `/education-diagnosis`。
- **FeatureUIRouter（纯规则）**：`decideFeatureUI` 决策树覆盖 typecheck；readiness empty→special_collection、partial+缺→light_followup、ready→result_view。
- **状态**：✅ **A 档 + C 档 docker 实测全通过**。C 档（pgvector + DashScope qwen-plus/text-embedding-v3）四轮走通完整状态机：笼统→special_collection、只讲上学日→light_followup（只问"周末最放松一段怎么过"）、答完→result_view（运转读取+3 张力点+周末留 30 分钟不可打扰时间）；psql 核验 4 memory_write+4 digest_update succeeded、4 条 `[教育诊断]` dailyUpdate、4 Episode 全带向量、atoms 18 parent_explicit+4 parent_inferred；响应无后台字段泄露。typecheck+build 通过。

### 功能-4 沟通预演 + 家庭规划 接入共用 5.3 基座
- **目标**：把已存在的预演/规划重构为单页状态机，与教育诊断统一 special_collection ↔ light_followup ↔ result_view；只增量改 API（加 uiMode），不破坏 conflict/daily 既有路径。
- **沟通预演**（`/rehearsal` + `/api/rehearsal/analyze`）：空原话+`fromSpecialFeature`→`special_collection`（引导补原话，5.2.3）；有原话→`result_view`+profileAware（childLikelyHearing/saferVersion/avoidPhrases）；采集写回 `[沟通预演]` dailyUpdate+Episode（仅标准入口、非 conflict）。
- **家庭规划**（`/family-planner` + `/api/family-planner`）：`enoughToPlan=false`→`light_followup`（只追问失败节点，5.4.3）；出计划→`result_view`（先稳边界+1-3 动作）。
- **回归**：`/conflict/result`(mode=conflict) 仍返回冲突复盘且**不带** uiMode/profileAware；`/rehearsal/input` 流式聊天不受影响。
- **状态**：✅ **C 档 docker 实测全通过**：预演空原话→special_collection、真原话→result_view profileAware；规划模糊→light_followup 追问失败节点、清晰→result_view 3 动作；conflict 回归正确（无 uiMode）；psql 核验 `[沟通预演]`×1+`[家庭规划]`×3 dailyUpdate、Episode×1（预演写回）、job 8 succeeded；无字段泄露。typecheck+build 通过。

### 功能-5 首次建模采集闭环去 mock + 打通闭环
- **目标**：五入口采集流从 mock 切真，L3 采集即写，final 补充真实参与综合，闭环 EntryEvidencePack→synthesis→FamilyModel→Brief→Board 端到端可追。
- **步骤 & 预期**（档位 C）：
  - `POST /api/entry/analyze {entryType:study, stage:summary}` → 真 mainJudgment+facts；psql `entry_evidence_packs` 即有该入口包（**L3 采集即写**，之前从不落库）；
  - `POST /api/entry/analyze {entryType:final}` → 返回**真追问**（非固定 mock）；
  - `POST /api/synthesis {entryMap, crossCuttingSupplement}` → ok、机制/跨入口非空、不报错；
  - 走完 generating → psql 确认 memory_write/digest_update succeeded、board_snapshots/family_memory_digests 生成；
  - 五入口 summary/follow-up + final 页**无 key/失败显示 loading/重试**（不出假数据）；mockOutputs 死导出已删。
- **状态**：✅ **C 档 docker 实测通过**：entry/analyze 写 L3（entry_evidence_packs=1 learning_homework）、final 真问题、synthesis 带 crossCuttingSupplement（5 机制 6 跨入口）、job 全 succeeded；typecheck+build 通过（10 页体积下降、无 mock 引用）。

### 功能-6 多视角校正 multi-view + child-voice 接真
- **目标**：唯一还整条 mock+丢弃输入的孤立流接真——孩子原话持久化、三视角（家长/孩子/老师）真生成、老师视角无数据时诚实不编造。
- **步骤 & 预期**（档位 C）：
  - `POST /api/multi-view {childText}` 无环境数据 → childView 用孩子原话、**teacherView 诚实"学校那边还不清楚"**、无字段泄露；
  - 先 `entry/analyze {entryType:environment, stage:summary}` 建环境入口 → 再 multi-view → **teacherView 用真老师事实**（课堂能跟上、较少举手、被动配合）；
  - psql：`[孩子视角]` dailyUpdate 写入、Episode 写入；
  - profile/result 有「听听孩子自己怎么说」入口可跳 child-voice；child-voice 提交后多视角即时用其原话。
- **状态**：✅ **C 档 docker 实测通过**：无数据→teacherView 诚实降级、有环境入口→真老师观察、孩子原话写回 `[孩子视角]`×2、headline 点出真实三方差异、无字段泄露；typecheck+build 通过。

### 功能-7 周报 weekly-review GET/POST 契约修复
- **目标**：前端 POST 期望 AI 周报、后端只有 GET（缺 POST handler）→ 周报永远 mock。补 POST 真生成 + 前端去 mock。
- **步骤 & 预期**（档位 B，仅需 LLM key 无需 DB）：
  - `POST /api/profile/weekly-review {observations:[...]}` → 返回 `{headline,summary,repeatedPatterns,keyObservation,nextWatchPoints}`，headline 是倾向描述非"更懒了"标签；
  - 空 observations → 400 NO_OBSERVATIONS；无 key → 503（前端显示重试，不出假数据）；
  - GET 保留不动（api-client 消费方不受影响）。
- **状态**：✅ **C 档 docker 实测通过**：POST 真观察→真周报（4 模式+keyObservation+3 watchPoints）、空观察→400；前端去 mock 改 loading/重试；typecheck+build 通过。

### 功能-8 孩子画像跨设备/重装持久化
- **目标**：画像快照持久化到 DB，新设备/重装后可取回（此前只存 localStorage）。
- **步骤 & 预期**（档位 C）：
  - 设备A `POST /api/profile/built {snapshot}` → saved；
  - 设备B（localStorage 空）`GET /api/profile/built` → 完整取回 completeness/coreJudgment/evidence/verificationPoints；
  - psql `built_profile_snapshots` = `f:c:latest` 单例、按租户隔离；
  - home 已有画像时入口卡变「孩子画像·去查看」→/profile/result；result/home 本地空时 DB-first 回灌 localStorage（hasProfile 全应用生效）。
- **状态**：✅ **C 档 docker 实测通过**：设备A 写入→设备B 取回、单例隔离正确；typecheck+build 通过。

### 功能-9 材料理解 material-understanding（交付文档 2 / 8.1 缺失功能补齐）
- **目标**：补齐文档明确但代码缺失的「材料理解」功能（文档 2 节功能表 + 8.1 MaterialUnderstandingAgent）。家长贴入材料文本（老师反馈/作业/录音转写/截图文字）→ 前台 Agent 给家长可读解读（区分事实 vs 评价）→ 后台把材料抽成事实并标 `sourceType=material_observation` 写入记忆，驱动 Board/Brief。文本优先 MVP，不含图片/OCR（phase 2）。
- **范围**：独立专项页 `/material-understanding` + 首页第 4 卡（专项卡区 3→2×2）；新前台 prompt `materialUnderstanding`（18→19）；`IngestContext.materialSource` + episodeExtractor 材料判定规则；复用 episode_ingest/memory_write 链路（零新基础设施）。
- **步骤 & 预期**：
  - **A 档（无 key/DB，prod server.js）**：
    - 匿名 POST `/api/material-understanding` → 401（middleware 拦截）；
    - 伪造 cookie + 同源 → 401（`getCurrentUser` 会话校验失败）；
    - 真实 demo 会话 + 同源 → **200**，返回 `{traceId, reading, keyPoints}`；无 key 时 reading 为中性降级文案、keyPoints=[]（不出假解读）；
    - `/home` SSR 含 4 专项卡（沟通预演/教育模式诊断/家庭规划/材料理解）+ `feature-quad` 2×2；`/material-understanding` SSR 200，渲染标题/类型 chips/副标题。
  - **C 档（Docker pgvector + DashScope key，已实测✅）**：POST 老师反馈材料 → psql `fact_atoms` 有 `source_type=material_observation` 高价值原子（embedding 非 NULL）、`evidence_episodes` 有 Episode、`memory_layer_items` 有 `[材料理解]` dailyUpdate；该 traceId 两 job succeeded；前端 reading/keyPoints 为真解读。
- **状态**：✅ **A 档 prod server.js 实测通过**：鉴权三态（匿名/伪造/真实会话）正确、200 契约形状正确、降级文案不出假数据；首页 4 卡 + 专项页 SSR 渲染正确；typecheck+build（19 prompt）+ 乱码扫描通过。✅ **C 档全套实测通过（dev+真 pgvector+有效百炼 key）**：POST 老师反馈材料 → materialUnderstanding agent 真解读(reading+3 keyPoints) → psql `fact_atoms` 有 **2 个 `material_observation` 原子(都带 embedding)**（"上课注意力不集中"/"作业两次没交"）+ 孩子原话标 `child_quote`(带 embedding) + `evidence_episodes` 1 行带向量 + `[材料理解]` dailyUpdate 入库。materialHint→episodeExtractor 规则完全生效。

### 功能-10 判断差量 + 孩子理解分析卡搬进对话页（交付文档 4.5）
- **目标**：交付文档 4.5 把「判断差量」「初版/标准/深度孩子理解分析卡」列为 daily 输出形态，但此前只进家庭看板（boardUpdater judgmentChanges），对话页连后台已发的 linkedAreas 都丢弃。本轮把判断差量 + 理解卡作为家长可读卡片搬进 /daily（混合·折叠形态），看板判断变化段保留（累积复盘，与对话页本轮即时差量互补）。
- **设计**：零额外 LLM、纯函数装配——`buildDailyCards(output)`（pipeline.ts）从同步 OrchestrationOutput 取数：判断差量来自 `relationshipToExistingModel.type`（counter_evidence/new_mechanism_signal 才给）、理解卡 reading 用 `relevantChildStructureModel[0]`（/profile/result 在用的家长可读 childTendency）、档位按 `contextMaturityLevel`（L2初版/L3标准/L4深度）。daily/stream final 事件 + daily 非流式 data 加 `cards`；前端 `AiTurnExtras` 渲染判断变化内联卡 + 折叠理解卡 + linkedAreas chips。
- **P0 红线**：cards 文案全程硬编码/已有家长可读文本，不暴露字段名/置信度/机制名；安全回复不挂卡片。
- **步骤 & 预期**：
  - **A 档（无 key/DB，prod server.js）**：匿名 stream → 401；真实 demo 会话+同源 stream → 200，NDJSON `final` 事件**含 `cards` 字段**（无 profile 时 `{}`，空安全）+ `linkedAreas` 恢复（之前被前端丢弃）；非流式 `/api/daily` data 含 cards；前端 cards 空时 AiTurnExtras 返回 null 不渲染多余卡片。
  - **C 档（pgvector + DashScope key，已实测✅）**：先建画像 → daily 普通输入 → `cards.understandingCard` 有 reading + 正确 tier；反证类输入（「他今天反而主动拿题问我」）→ `cards.judgmentDelta` 出现；前端 AI 回复后内联「判断有更新」卡 + 折叠「当前对孩子的理解·{档}」+ chips；看板「判断变化」段仍在（互补）。
- **状态**：✅ **A 档 prod server.js 实测通过**：匿名 401、真实会话 stream 200 含 cards 字段(空安全)+linkedAreas 恢复(["学习与作业","亲子沟通"])、非流式 data 含 cards、降级文案正常；typecheck+build（19 prompt 无新增）+ 乱码扫描通过。✅ **C 档卡片真内容实测通过（dev+真 pgvector+有效 key）**：建画像后 daily 普通输入 → `understandingCard`(tier=标准/L3 + 真 childTendency reading "当家长在未协商前提下发起学习相关反馈…孩子…")；反证输入「他今天反而主动拿题问我」→ `judgmentDelta` 命中("这条和之前的理解不太一致…")；看板「判断变化」段同时有 2-3 条累积复盘 → **对话即时差量 + 看板累积复盘互补成立**。

### 工程-7 TurnEvent 输入快照（交付文档 7.2 字段闭环可追溯/可复现）
- **目标**：补 per-turn 输入快照——daily 每轮把「喂给 Agent 的上下文 + Agent 产出」按 traceId 持久化（layer_name='turn_events'、item_id=traceId 复用 memory_layer_items，零迁移），并提供按 traceId 取回的审计接口，满足 7.1 可追溯 / 13.1 可复现。retrievedContextSnapshot 即文档 familyBriefSnapshot+retrievedFactsSnapshot 的本架构映射；recentTurnsSnapshot=[]（daily 无会话缓冲）、knowledgeContextSnapshot=null（9.2 预留）、retrievedFactIds 暂缺（检索只返文本不返源 ID）。
- **设计**：`TurnEvent` 类型(database.ts)；`buildTurnEvent(output...)` 纯函数装配(pipeline.ts，零额外查询)；`saveTurnEvent/getTurnEventByTraceId/listTurnEvents`(database-manager.ts，复用层 CRUD，DB-off 进程内存同源)；daily/stream + daily 两路由 final 后 `void saveTurnEvent`(fire-and-forget 不阻塞)；`GET /api/turn-events?traceId=`(verifyAppApi+resolveTenant 本租户限定)。
- **步骤 & 预期**：
  - **A 档（无外部 DB，进程内存 store 即可验完整闭环）**：匿名 GET turn-events → 401；demo 会话+同源 POST daily/stream → 拿 traceId → GET turn-events?traceId= → **返回该轮 TurnEvent 含 userMessage/assistantReply/maturityLevel/relationship/retrievedContextSnapshot(8字段)/routingDecisionSnapshot/memoryActionSnapshot**、recentTurnsSnapshot=[]/knowledgeContextSnapshot=null；GET turn-events 列最近含该轮；非流式 /api/daily 同样落 TurnEvent(mode=daily_dialogue)。
  - **C 档（pgvector，已实测✅）**：psql `memory_layer_items` 有 `layer_name='turn_events'`、`item_id=traceId` 行；进程重启后仍可 GET 取回（真持久化）。
- **状态**：✅ **A 档 prod server.js 实测全通过**：匿名 401；daily/stream 拿 traceId→GET 取回完整快照(输入+输出+检索上下文+路由决策，可复现成立)；列最近含该轮；非流式 daily 也落 TurnEvent；typecheck+build(19 prompt)+乱码扫描通过。✅ **C 档真 Postgres 落库实测通过**（dev 模式连真 pgvector：daily/stream 拿 traceId→GET 取回快照(retrievedContextSnapshot 8 字段全在)→psql 直查 `memory_layer_items WHERE layer_name='turn_events' AND item_id=traceId` 命中行、userMessage 完整）。仅 daily 落 TurnEvent（主入口），其它前台路由 buildTurnEvent/saveTurnEvent 通用接口后续各加一行接入。

### 工程-8 首次建模 v1 三件套强保证（交付文档 3.4 / 13.4）
- **目标**：文档 3.4 要首次建模产出 FamilyModel v1 + Family Brief v1 + BoardSnapshot v1，13.4 要 v1 链路跑通；此前 generating 跳 result 零等待，Brief/Board v1（memory_write→digest_update 异步产出）可能未就绪，家长立刻开 /board 见 pending、/daily 无 familyBrief。本轮让 generating 在跳转前等三件套 v1 就绪（带超时兜底，不死锁）。generating 本是 loading 页，等待不违背前台快。
- **设计**：新 `GET /api/profile/readiness`（verifyAppApi+resolveTenant 并行查 getLatestChildStructureModel 非空 + getCurrentVersions.briefVersion>0 + boardVersion>0，返 {familyModel,brief,board,ready}，本租户限定，区别于基础设施探针 /api/readiness）；generating 加第 4 步「生成家庭简报与支持看板」，built POST 改 await，diagnosis 后轮询 readiness（复用 board 轮询模式，~8×2s）ready 或超时都跳 result。
- **步骤 & 预期**：
  - **A 档（无 key/DB）**：匿名 GET /api/profile/readiness → 401；demo 会话+同源 → 200 `{familyModel:false,brief:false,board:false,ready:false}`（空租户契约正确）；/api/readiness 基础设施探针仍独立 200（路径不冲突）；generating build 通过、轮询+超时分支类型正确。
  - **C 档（pgvector + DashScope key，已实测✅）**：走完五入口 → generating synthesis/diagnosis 成功 → 第 4 步轮询 readiness false→true → 跳 result；跳转后立即开 /board 非 pending、/daily 能用 familyBrief；psql 确认 child_structure_models + family_memory_digests(brief_version≥1) + board_snapshots(version≥1) 三者皆有；制造 digest 慢/失败 → 轮询达上限仍跳 result（不卡死）。
- **状态**：✅ **A 档 prod server.js 实测通过**：匿名 401、demo 会话 readiness 200 三件套契约正确、/api/readiness 不冲突；typecheck+build(19 prompt)+乱码扫描通过。✅ **C 档 false→true 全流程实测通过（dev+真 pgvector+有效 key）**：readiness BEFORE `{familyModel:F,brief:T,board:T,ready:F}` → POST synthesis(200，5机制+6跨入口)+diagnosis(200，有 conditionalProfile) → 等 memory_write→digest 链 → readiness AFTER **`{familyModel:T,brief:T,board:T,ready:T}`**；psql child_structure_models=1 + family_memory_digests(brief_version=1) + board_snapshots(version=2) 三件套皆有；/api/board 真五段内容(疏离感/3稳定理解/2判断变化/具体下一步)非 pending。**刻意不做**：不改三件套产出链（只加等待+就绪端点）；超时不报错照常跳转（graceful degradation）。

---

## 3. 红线 / 体验测评（文档 11.2 / 4.4 · 人工抽检）

每次涉及前台文案改动时，人工抽检一组真实输入，逐条核对：

- [ ] 前台**无审判感**：不说家长控制欲强、不说孩子就是懒/摆烂
- [ ] **不泛泛建议**：不出现「多鼓励少批评」这类空话
- [ ] **不标签化 / 不医疗化**：不贴诊断标签、不医疗化归因
- [ ] **不暴露内部术语 / 置信度**：前台无机制名、confidence、内部 JSON
- [ ] 关键追问含「**在区分什么**」结构（文档 4.4）
- [ ] **事实 ≠ 评价**：家长评价（「懒」）不被当成孩子事实落库（核 FactAtom 的 sourceType=parent_inferred）

---

## 4. 回归测评矩阵（一览）

| 优化项 | 档位 | 主要验证手段 | 状态 |
|---|---|---|---|
| 通用门 G1-G3 | A | `npm run typecheck/build/prompts` | ✅ |
| P0-1/P0-2 | A | 字段抽检 + build | ✅ |
| P0-3 流式 | B/A | 流式逐行 / 降级文案 | ✅ / ⚠️ 真流式待复测 |
| 功能-1 规划 | B/C | `/api/family-planner` 清晰 vs 模糊 | ✅ |
| 功能-2 看板 | C | `/api/board` 五段 + version | ✅ |
| 架构-1 检索 | B/C | `test-vector-retrieval.mjs` / `verify-pgvector.mjs` | ✅ |
| 架构-2 Prompt | A | 改 md → rebuild → 无乱码 | ✅ |
| 架构-3 traceId | C | 三表 trace_id 对齐 SQL | ✅ |
| 体验-1 主入口/追问 | B | `/daily` 追问结构 | ✅ |
| 闭环-1 Brief/Board+3 缺口 | C | 链路 + 幂等 + 预演 + 规划写回 | ✅ |
| 审计-1（4 项） | C | 四点分别核查 | ✅ |
| 架构-4 job_queue | C | 8 场景状态机/幂等/重试 | ✅ / ⚠️ inline 降级未运行时测 |
| 质量-1 多租户 | C | 双用户隔离 + PK 回归 | ✅ |
| 质量-2 内存告警 | A | 降级日志 | ✅ |
| 质量-3 脚本归档 | A | `ls scripts/legacy` | ✅ |
| 质量-4 画像异步 | B/C | `/profile/generating` | ✅ |
| 功能-3 教育诊断+5.3基座 | A/C | API 三态 + 首页三卡 + 红线断言 | ✅ A+C 档 docker 实测全通过 |
| 功能-4 预演/规划接入5.3 | C | 预演/规划三态 + conflict 回归 + 写回 | ✅ C 档 docker 实测全通过 |
| 审计-2 专项 5 缺陷修复 | A/C | 规划无key门槛/教育priorTurns/三专项episode_ingest/ready空降级/预演自述维度 | ✅ A+C 档 docker 实测通过 |
| 功能-5 采集闭环去 mock | C | entry/analyze 写 L3 + final 真问题 + synthesis 补充 + 10 页去 mock | ✅ C 档 docker 实测通过 |
| 功能-6 多视角校正接真 | C | 三视角真生成 + 老师视角诚实降级 + 孩子原话写回 | ✅ C 档 docker 实测通过 |
| 功能-7 周报契约修复 | B | POST 真周报 + 空观察 400 + 前端去 mock | ✅ docker 实测通过 |
| 功能-8 画像跨设备持久化 | C | 设备A 写入 → 设备B 取回 + 单例隔离 | ✅ C 档 docker 实测通过 |
| 工程-2 前台快后台深 P0 | C | family-planner 不泄露/board pending 轮询/diagnosis 传 synthesisOutput | ✅ C 档 docker 实测通过 |
| 工程-3 entry-evidence 后台化 | C | 前台只返阶段反馈 + 后台深拆证据包(评价/事实分离) | ✅ C 档 docker 实测通过 |
| 工程-4 假设复核 Agent(P2-①) | C | 复核给 weight/status/反证, "不自觉"被反证削弱 | ✅ C 档 docker 实测通过 |
| 工程-4 定期重评(P2-②) | C | daily 新证据按 10min 桶触发 model_review 重评假设 | ✅ C 档 docker 实测通过 |
| 工程-5 隔离/健壮性 3 项 | A/C | 登录态清本地缓存/generating 错误重试/model_review 旧数据兜底 | ✅ 旧数据 docker 实测 + build |
| 工程-6 闭环可观测(P2-④) | C | GET /api/jobs/status job 健康度 + board 展示 evidenceRefs | ✅ C 档 docker 实测通过 |
| 体验-1 全输入语音化 | C | VoiceFieldButton 铺 12 输入面 + 全局单活麦克风 + 连不上降级；2 共享组件惠及 3 页 | ✅ build + Docker SSR 渲染验证（真转写须 server-ws/asr-proxy 环境） |
| 审计-3 P0/P1/P2 核验修复 | A/C | 10路并行核验→修 snapshot鉴权/IDOR/假画像、board补门、verifyInternalApi收紧、family-profile/record-child/3个mock页去假 | ✅ prod server.js 实测：匿名401/登录后snapshot 503无假画像/jobs鉴权按token | 
| 审计-3b P0-2b session 真校验 | C | verifyAppApi 改 async 校验会话(getCurrentUser)，18 调用点全加 await，删旧 hasSessionCookie | ✅ prod 实测：无cookie/伪造cookie 401、真实演示会话 503；grep 确认零漏 await |
| 审计-3c P1P2 删死字段 | A | 3路并行追踪证实 OrchestrationOutput.decomposedInput 死字段(误判)，删字段+2死函数；synthesis 消费的是同名 EntryEvidencePack.decomposedInput | ✅ tsc+build 通过，零残留；纯删死代码无运行时变化 |
| 审计-4 第二批安全批 | C | 全路由普查→11 条缺守卫路由补 verifyAppApi(含 asr-token 泄露凭证)；jobs/status verifyInternalApi 回归(no-token prod 改拒绝) | ✅ prod 实测：伪造cookie 5类路由全401、jobs伪造Referer 401、真实演示会话record-child 200 |
| 审计-4b record-child 联动 Board | C | 加 memory_write 入队(→digest_update→Board)；resolveTenant 替 body 身份堵 IDOR；旧同步写保留兼容 | ✅ tsc+build；prod 演示会话 200、伪造cookie+body越权 401、DB-off inline 未炸 |
| 审计-4c 日常深拆 Layer 1 | C | 修 linkedAreas 恒空(deriveLinkedAreas 主题标签)；Board 加判断变化分区(judgmentChanges) | ✅ prod 实测 linkedAreas 非空(学习/手机/情绪/沟通)、/api/board 含 judgmentChanges 字段；tsc+build |
| 审计-4c 日常深拆 Layer 2 | C | daily_deep job + dailyDecompose agent：六维深拆+保守生成假设(去重)→memory_write→model_review→Board | ✅ tsc+build(18 prompt)；prod daily 200、inline no-op 不炸；全链需 DB+LLM 验 |
| 审计-4d conversation store 租户隔离 | C | getCachedOrPersistedConversation 加 tenant 归属校验；9 会话函数透传 tenant、9 路由 resolveTenant() | ✅ prod 实测本租户会话 200、非己/不存在 404；tsc 保证 9 调用点无遗漏 |
| 审计-4e 底栏导航对齐 5 项 | C | BottomNavTabs 5项(对话/预演/诊断/记录/看板)+talk-tabs-five；首页换共享组件；规划/档案二级入口(诊断结果+看板) | ✅ prod 实测 home 渲染 talk-tabs-five+5标签、旧 six 移除；tsc+build |
| 审计-5a memory/write schema bug | C | 后端把 string[] 规整为合法 RawMaterial/CleanedFact 对象，修 materialId=undefined 索引污染 | ✅ prod 实测 demo+string[] payload 200；tsc+build |
| 审计-5b middleware 白名单收窄 | C | PUBLIC_PATHS 收到 login/health/readiness；middleware 加内部 token 接受 | ✅ prod 实测：公开200/匿名401/Bearer正确200错误401/demo同源200 |
| 审计-5c 关联领域扩词 + 文档计数 | A | deriveLinkedAreas 扩学科/学习困难词；docs prompt 11→18(5处)+TurnEvent映射 | ✅ tsc+build；doc 同步 |
| 审计-6 record-child 双写下线 | C | 删旧 insertMemoryRecords+rebuildFamilyMemoryDigest，单一走 child_events+episode+memory_write→Board；shouldWrite 门控 | ✅ prod 实测 record-child 200、单一路径不炸；archive/problem 流旧路不动；tsc+build |
| 功能-9 材料理解（缺失功能补齐） | A/C | prod 鉴权三态+200契约+降级；首页4卡/专项页 SSR；IngestContext.materialSource→material_observation | ✅ A 档 + ✅ C 档全套（2 个 material_observation 原子带 embedding、child_quote 分离、Episode 向量、dailyUpdate） |
| 功能-10 判断差量/理解卡搬进对话页 | A/C | buildDailyCards 纯函数装配 final.cards；前端 AiTurnExtras 判断变化卡/折叠理解卡/chips；看板互补 | ✅ A 档 + ✅ C 档卡片真内容（understandingCard 标准档真 reading、反证输入 judgmentDelta 命中、看板 judgmentChanges 互补） |
| 工程-7 TurnEvent 输入快照 | A/C | buildTurnEvent 装配 + saveTurnEvent(turn_events 层)；daily 两路由落点；GET /api/turn-events 审计 | ✅ A 档 prod 闭环 + ✅ C 档真 Postgres 落库实测（psql 直查 turn_events 行命中） |
| 工程-8 首次建模 v1 三件套强保证 | A/C | GET /api/profile/readiness 三件套就绪；generating 第4步轮询+超时兜底跳转 | ✅ A 档 + ✅ C 档 false→true 全流程（synthesis/diagnosis→model→readiness:true、三件套 version≥1、board 真五段） |

---

## 5. 待测 / 长期观察项

- **P0-3 真流式**：需在配 `FAST_AI_API_KEY` 的环境复测逐 token 流式体验（当前 A 档走规则降级）。
- **job_queue inline 降级**：DB 未启用时的 inline 回退仅代码验证，未运行时实测。
- **poller 多实例**：当前仅 pm2 单实例假设；多实例部署需加分布式锁后再测。
- **生产 pgvector**：生产 DB 须预装/有权限装 `vector` 扩展；无扩展自动降级到应用层 cosine（功能不退化但无加速）。
- **mock/localStorage 剩余残留（独立一轮）**：~~五入口采集流~~ → **功能-5**；~~multi-view/child-voice~~ → **功能-6**；~~weekly-review 契约~~ → **功能-7**；~~profileSnapshot 多端持久化~~ → **功能-8**。剩余：conflict-result/observation 的合理 mock fallback（AI 失败降级，保留）。---

*最后更新：2026-06-19（含 C 档完整复测：dev+真 pgvector+有效百炼 key，4 批内容项全验）｜ 与优化清单同步维护。新增功能时务必同时更新两份文档。*
