# 育见 ChildOS 优化清单

> 依据：《育见 ChildOS 完整产品交付文档（新版）V2.0》对照当前代码实测。
> 结论：交付文档描述的是 **V2.0 目标架构**，当前代码处于**重构前状态**，完成度约 70%。
> 维护：完成一项后在「状态」栏打勾并记录提交。
> **双文档同步**：每完成一项功能/修复，必须同时更新 [`测评清单.md`](测评清单.md)（补该项的操作步骤+预期+状态），两份文档配套维护。

---

## 图例

| 标记 | 含义 |
|---|---|
| 🔴 P0 | 违反交付文档明确红线 / DoD，必须修 |
| 🟠 架构 | 文档核心机制未落地，工程量中大 |
| 🟡 功能 | 文档定义但代码缺失的功能模块 |
| 🟢 质量 | 工程隐患，文档第 10.4 / 14 节要求规避 |
| ✅ | 已完成 |

---

## 第一批：P0 红线（已完成）

### ✅ P0-1 前台输出收敛为 visibleReply + traceId 🔴
- **问题**：`app/api/daily/route.ts` 原样返回整个 `orchestration` 对象，内含 `decomposedInput`、`routingDecision`、`memoryAction`、`relationshipToExistingModel`（含 confidence）等内部判断字段。
- **违反**：交付文档 6.5（前台只输出 `traceId + visibleReply`）、11.2（禁止暴露内部术语/置信度）、13.3 P0（前台输出复杂 JSON）。
- **修复**：route 收敛为 `{ ok, data: { traceId, visibleReply, linkedAreas } }`。`linkedAreas` 为家长可见的关联领域名（衍生展示，非内部推理），内部字段全部留在服务端。
- **顺带修复既有 bug**：前端 `app/observation/page.tsx` 原读 `orchestration.frontResponse`，而后端字段名是 `frontResponseDraft`——**字段名不匹配导致 AI 回复一直拿不到、永远 fallback 到 mock**。现已对齐为 `visibleReply`。
- **验证**：`npm run typecheck` 通过、`npm run build` 通过。

### ✅ P0-2 后台记忆写入异步化 🔴
- **问题**：`app/api/daily/route.ts:40` 在返回响应前 `await runMemoryWritePipeline()`，用户必须等记忆写入完成才看到回复。
- **违反**：交付文档 6.3（后台任务不得阻塞用户看到回复）、12.4、14.3。
- **修复**：改为 fire-and-forget（`void ...catch()`），先返回回复，记忆写入在后台执行；写入失败只记日志、不影响前台。当前部署为 pm2 fork 长驻进程，该方式安全可靠。
- **遗留**：失败重试机制留待 job_queue 改进（见 架构-4）。

---

## 第二批：核心链路（✅ 已完成）

### ✅ P0-3 日常对话前台流式输出 🔴（已完成 · commit 09bf368）
- **关键现状**：daily 此前是**纯规则引擎**（`orchestration/pipeline.ts` 未接 LLM），回复是规则模板。
- **实现**：新增 `app/api/daily/stream/route.ts`（NDJSON 流式）——规则引擎做检索/判断/安全检测/降级文案，LLM（`dailyDialogueOrchestration`）流式生成 visibleReply；**无 AI key 或失败时降级为规则文案，行为不退化**；安全风险输入直接走规则安全文案。前端 `observation` 改为逐行消费流、实时展示。后台写入异步。
- **注意**：真流式 LLM 效果需在配置了 `FAST_AI_API_KEY` 的环境验证；当前无 key 环境走规则降级路径，已通过 typecheck + build。旧 `/api/daily`（非流式）保留未删。

### ✅ 功能-1 家庭综合规划 family_planner 🟡（已完成 · commit 5fd151a）
- **实现**：新增 `app/api/family-planner/route.ts`（`familyPlanner` prompt + LLM + 降级文案，actions 强制收敛 1-3 个）+ `app/family-planner/page.tsx`（场景化自述工作台：大输入框 + 提示卡，符合 5.4.1）+ home 底部导航入口。输出严守 5.4.3「不给密集时间表、先稳边界、失败原因不清先追问」。前台只返回 `plan + traceId`。

### ✅ 功能-2 家庭支持看板 board 🟡（已完成 · commit 9d03d81）
- **实现**：新增 `app/api/board/route.ts`（`boardUpdater` prompt，五段式：当前状态/稳定理解/互动模式/待验证点/下一步 + 温和空态降级）+ `app/board/page.tsx`（五段卡片，数据全部来自后端）+ home 顶部入口卡。看板由后台生成、不由前端伪造；信息不足时不硬编画像。独立于现有 `family-profile` 档案页，零侵入。

---

## 第三批：架构深化

### ✅ 架构-1 向量检索（检索时精排版）🟠（已完成 · 实测提升 27%）
- **已做**：新增 `src/lib/server/memory/embedding.ts`（阿里百炼 `text-embedding-v3` 客户端，1024 维，自动分批 ≤10，`cosineSimilarity` + `rankByRelevance`）。`buildDailyDialogueRetrievalPacket(query)` 从「取最近 5 条」升级为「从全部历史事实按语义相关性精排」；`orchestration/pipeline` 传入 `userText` 作为 query；embedding 未配置时自动降级为「取最近」，功能不受影响。
- **实测**：用真实 key 跑 `scripts/test-vector-retrieval.mjs`，向量精排 top5 平均相关度 **0.61 vs 取最近 0.48（+27%）**；并通过实测发现并修复了「批量上限 10 条」的真实 bug。
- **遗留（后续可选）**：当前为检索时**实时编码**（无持久化），数据量大时应改为写入时预编码 + pgvector 索引（维度 1024 适配 pgvector），避免重复编码。
- **✅ 已升级（commit 6dde9e3）**：实时编码已被「**EvidenceEpisode + FactAtom + pgvector 三层语义检索**」取代——每轮输入后台 LLM 抽取语义完整 Episode（总向量化，主召回单元）+ FactAtom（标 `source_type`，仅高价值单独向量化）；检索三段式（标签过滤 + 向量 topK + 证据/时间加权精排）；三级降级链（pgvector→应用层 cosine→取最近）。新增两层为检索专用，现有 10 层与画像生成链路不动。docker pgvector 端到端验证通过：Episode 语义完整召回（非碎片）、来源分层正确、选择性向量化生效、跨用词语义召回（query「逃避」召回「背不会/摔本子」场景）。

### ✅ 架构-2 Prompt 外置为 md 文件 🟠（已完成 · md 源 + 构建期生成）
- **已做**：11 个 prompt 抽到 `prompts/{front,background}/*.md`（文件名即 key，对齐文档 8.2 目录化），作为可编辑源；`scripts/build-prompts.mjs` 在 `predev`/`prebuild`/`pretypecheck` 把 md 编译为 `src/lib/server/prompts/registry.generated.ts`（bundled 进 .ts，**零运行时 fs、零 standalone/rsync 部署风险**）；`agent-prompts.ts` 改为 import 注册表 + 保留 `problemJudgment`，`AgentPromptKey` 类型与所有调用点不变。
- **为何不用运行时读 md**：Next `output:standalone` + rsync 部署下，运行时 `fs.readFileSync(md)` 需 `outputFileTracingIncludes` 且 deploy 必须 rsync `prompts/` 目录，否则生产 prompt 加载失败、无法完整验证。构建期生成把 md 编译进 bundle，规避此风险，仍保留 md 的可编辑/可 diff/目录化。
- **验证**：提取脚本字节级抽取（无乱码）；prebuild 生成 11 prompt；tsc + build 通过；内容完整性核对（dailyDialogue 的 Q2 追问硬规则、episodeExtractor 来源底线、familyPlanner 1-3 动作约束均保留）。改 md → rebuild 自动重生成。

### ✅ 架构-3 traceId 全链路字段闭环 🟠（已完成 · docker 三方对齐验证）
- **现状**：用 10 层 memory + `ChildStructureModel`(L5)、`DailyInteractionUpdate`(L9) 替代 FamilyModel/TurnEvent，实际比文档更细，**不硬套文档命名**。
- **已做**：补齐 `traceId` 全链路贯穿（文档 7.1 字段闭环）——`DailyInteractionUpdate.sourceEventId` + `createDailyUpdate(...,traceId)` 持久化到记忆层；`job_queue` 加 `trace_id` 列 + `enqueueJob(...,traceId)`；daily/stream 把同一 traceId 传给 memory_write + episode_ingest 两个 job。
- **验证**：一条家长消息 → docker 实测三方对齐：job_queue 两 job 共享同一 `trace_id`、`daily_updates.sourceEventId` = 该 traceId、`evidence_episodes.source_event_id` = 同一 traceId，全链路可 `SELECT ... WHERE trace_id=X` 追溯。

### ☐ 体验-1 对话主入口 + 追问结构化（本轮一并修复）🟡
- **对话主入口已切新版**：首页提交从旧 `/problem/follow-up` 改为新建 `app/daily/page.tsx` 多轮日常对话页（消费 `/api/daily/stream`，合并深度复盘成主入口，文档 4）；旧 problem 流程保留。
- **追问"在区分什么"已结构化**：规则层 `buildFrontResponse` 的 `one_key_followup` 文案改为显式「这里想区分 X 还是 Y」（对齐文档 4.4 示例）；`dailyDialogueOrchestration` prompt + daily/stream task prompt 加硬约束「提关键追问必须先说明在区分什么」。docker 实测 LLM 回复以"这里想区分…"开头。

### ✅ 闭环-1 Brief/Board 后台闭环 + 3 个前台读写缺口 🔴（已完成 · docker 端到端验证）
- **P0 Brief/Board 闭环（文档 7.5/7.6/12.1/13.4）**：新增 `memory/digest/updaters.ts`（FamilyBriefUpdater + BoardUpdater）+ `digest_update` job（memory_write 成功后链式 enqueue）；持久化 `board_snapshots`（version/evidenceRefs/content_hash）+ `family_memory_digests` 补 brief_version/hash/evidence；`board/route` 改为**读持久化快照**（含 version/evidenceRefs/updatedAt），无则回退实时生成 + 自愈入队。**幂等关键**：content_hash 驱动——同证据集重跑→同 hash→`DO NOTHING` 不增版本，无 key→callAgentJson undefined→提前返回 changed:false（非失败）。docker 验证：daily→memory_write→digest_update 链全 succeeded、board_snapshots version=1 带真实 LLM 内容、board API 读到 version/evidenceRefs、同内容重发 version 不 +1。
- **P1 L0 降级追问**：规则降级（无 key）的 L0 兜底文案两处加「区分整体回避 vs 单点敏感」框架（pipeline.ts）。
- **P1 沟通预演 ContextPack**：rehearsal 页采集真正目标/担心反应/前置背景 → result 页发 `rehearsalContext` → analyze API 据此 + 服务端记忆检索（画像/过往类似沟通）走 profile-aware 路径并切换渲染分支。docker 实测 profileAware=true 且 saferVersion 引用了记忆中的真实家庭历史。
- **P1 家庭规划读写记忆**：`/api/family-planner` 改为读 `buildDailyDialogueRetrievalPacket`（既往尝试/承受力边界）喂 LLM + 把规划产出 `enqueueJob('memory_write')` 写回记忆（链式触发 digest）。docker 实测写回 `[家庭规划]` daily_update。

### ✅ 审计-1 文档符合度审计修复（4 项）🔴（已完成 · docker 验证）
> multi-agent workflow 对照交付文档审计出 15 缺口（去重 6 P0），修复其中 4 项高性价比的；另 2 项需产品决策的暂缓（见末尾）。
- **知识库 schema 预留（P0/9.2）**：`database.ts` 加 `KnowledgeCard`/`KnowledgeContext`（忠于文档 9.2 字段）+ daily/stream 前台输入预留 `knowledgeContext` 占位（P0 恒空，不接逻辑，不替代家庭事实）。
- **matchedMechanisms 语义错配（实质 P0 数据污染）**：daily 的 `createDailyUpdate` 原把 `supportingEvidence` 证据文本灌进 L9 的 `matchedMechanisms` 槽位。修复：`RetrievedContext` 加 `matchedMechanisms` + orchestration 透传 + daily 改传真正机制。docker 实测 `daily_updates.matchedMechanisms=[]`（不再污染）。
- **家庭规划失败原因门槛（P0/5.4.3）**：`familyPlanner` prompt 加 `enoughToPlan` 标志（单次调用，不加额外 LLM）——失败节点不清楚时 actions=[] 只追问失败节点；无 key 放行不卡死。docker 实测：模糊输入→追问失败节点、清晰失败原因→正常出动作。
- **沟通预演缺原话门槛（5.2.3）**：rehearsal/analyze 入口加引导式门槛，空原话返 `NEED_PARENT_WORDS` 而非 BAD_REQUEST。docker 实测。
- **✅ 已决策（A）·L1/L2 由 Episode/Atom 取代**：daily 流不写老 10 层的 L1(RawMaterial)/L2(CleanedFact)，因为新的 **EvidenceEpisode + FactAtom 链已是事实权威层**——Episode 存语义完整场景（含 sourceEventId 溯源，等价 L1 原话存档），FactAtom 带 `sourceType`（parent_explicit/parent_inferred/child_quote/material_observation/system_hypothesis）把"事实 vs 评价"分得比老 L2 更细，且向量化可语义检索。坚持双写 L1/L2 只会造成事实冗余 + 检索去重负担，无收益。文档 7.3 的"原话不可覆盖、家长评价不当孩子事实"由 Episode/Atom 满足。

### ✅ 功能-3 教育模式诊断独立入口 + 三专项功能共用 5.3 UI 切换基座 🟡（已完成）
> 交付文档 5.3 + 原型 image1：三个并列专项功能（沟通预演/教育模式诊断/家庭综合规划）共用「专项采集 UI ↔ 轻追问 UI」动态切换。教育诊断此前完全缺失，本轮新建并搭好共用基座。
- **共用基座**：`src/types/feature-ui.ts`（SpecialFeatureType/ContextNeedLevel/ContextReadiness/FeatureUiMode/状态机/SpecialFeatureContextPack/Router I/O）+ `src/lib/server/features/feature-ui-router.ts`（`decideFeatureUI` 纯规则决策树，文档 5.3.6；`deriveContextPack`/`normalizeReadiness`）+ 共用组件 `SpecialCollectionView`（重采集）/`LightFollowupView`（轻追问气泡）。
- **教育诊断垂直切片**：prompt `prompts/front/educationDiagnosis.md`（key=educationDiagnosis，输出 readiness/missingHighImpactFacts/modeReading/keyTensions/lightFollowupPrompt/collectionGuide，采集 7 要素）+ API `app/api/education-diagnosis/route.ts`（检索→LLM 判 readiness→Router 定 uiMode→写回 `[教育诊断]` dailyUpdate+ingestEpisode）+ 单页状态机 `app/education-diagnosis/page.tsx`（special_collection/light_followup/result_view 三态）+ 检索 `buildEducationDiagnosisRetrievalPacket`。
- **轻量持久化（决策 A）**：不建新表，readiness 由 LLM 单次判定，已采集事实经 Episode/dailyUpdate 累积，下轮检索带出。
- **三并列入口**：首页新增「专项功能」三卡区块（对齐原型）+ 底栏教育诊断第 6 项 + prefetch。
- **红线**：前台响应不含 readiness/coverage/confidence 等后台字段；信息不足只追问不硬生成；轻追问只问一个点不做问卷。
- **验证**：typecheck + build 通过（两路由编译）；**docker C 档（pgvector + DashScope qwen-plus/text-embedding-v3）端到端实测通过**——四轮对话走通完整状态机：笼统输入→`special_collection`、只讲上学日→`light_followup`（只问一个关键点"周末最放松的一段怎么过"+说明在看"结束感"）、补全→仍谨慎 light_followup、答完→`result_view`（运转读取+3 张力点+小步下一步）；写入链 psql 核验：4 memory_write+4 digest_update 全 succeeded、4 条 `[教育诊断]` dailyUpdate、4 Episode 全带向量、fact_atoms 18 parent_explicit+4 parent_inferred（事实≠评价分层正确）；前台响应无后台字段泄露。A 档无 key 降级亦通过。
- **本轮不含**：预演/规划接入共用 Router（现状已能用，留下轮）；教育诊断深度写回 FamilyModel（仅写 dailyUpdate+Episode）。

### ✅ 功能-4 沟通预演 + 家庭规划 接入共用 5.3 切换基座 🟡（已完成）
> 把已存在的沟通预演（5.2）与家庭综合规划（5.4）重构为单页状态机，与教育诊断统一 special_collection ↔ light_followup ↔ result_view；只增量改 API（加 uiMode），不破坏既有 conflict/daily 路径。
- **沟通预演**：`app/rehearsal/page.tsx` 重构为单页状态机（复用 SpecialCollectionView+LightFollowupView+内联 profile-aware 结果卡）；`app/api/rehearsal/analyze/route.ts` 增量——放宽 profile-aware 触发（标准入口 `fromSpecialFeature` 默认画像感知，拉服务端记忆）、空原话返回 `uiMode=special_collection` 引导（5.2.3）、成功加 `uiMode=result_view`、采集写回 `[沟通预演]` dailyUpdate + ingestEpisode（仅标准入口、非 conflict）。
- **家庭规划**：`app/family-planner/page.tsx` 重构为单页状态机（复用计划卡）；`app/api/family-planner/route.ts` 增量加 `uiMode`——`enoughToPlan=false`→light_followup（追问失败节点，5.4.3）、出计划→result_view。
- **保留不动**：`/rehearsal/input`+`/api/rehearsal/stream`（daily 追问聊天）、`/conflict/result`、`/rehearsal/result`（旧路由）。analyze 改动全为增量，conflict 路径不带 uiMode/profileAware。
- **采集态折叠**（用户确认）：预演采集从「原话+3 结构选填」折叠为单一自述+引导，体验对齐教育诊断。
- **验证**：typecheck+build 通过；**docker C 档实测全通过**——预演空原话→special_collection、真原话→result_view profileAware（childLikelyHearing/saferVersion/avoidPhrases）；规划模糊→light_followup 追问失败节点、清晰→result_view 3 动作；conflict 回归正确（headline+escalationSentence，无 uiMode）；psql 核验 `[沟通预演]`×1+`[家庭规划]`×3 dailyUpdate、Episode×1（预演写回）、job 8 succeeded；两 API 无后台字段泄露。
- **本轮不含**：SpecialFeatureContextPack 跨会话持久化（仍轻量实时推导）；`/rehearsal/input` 流式聊天并入状态机（属 daily 链路）。

### ✅ 审计-2 专项功能 5 处缺陷修复 🔴（已完成 · docker 验证）
> 用户审计出 6 个问题，其中 5 个是 5.3 接入工作引入的真缺陷，本批修复；第 6 个（mock 残留）是独立大重构，另列。
- **① 家庭规划无 key 硬出计划（违反 5.4.3 门槛）**：`insufficient = !ai || ai.enoughToPlan===false`——LLM 不可用时也判定不足→light_followup 追问失败节点，不再用 FALLBACK 拼泛泛计划。docker A 档实测：无 key 清晰输入→light_followup、0 动作。
- **② 教育诊断轻追问丢上下文（异步写入竞态）**：页面累积本会话 `turns`，提交携带 `priorTurns`；API 喂给 LLM。消除"异步写入未落库→下轮检索不到→状态机断"。docker 实测：turn1 只讲上学日→special_collection，turn2 带 priorTurns 补周末→light_followup（看到累积上下文才推进）。
- **③ 沟通预演折叠自述维度未被利用**：折叠后 rehearsalContext 不结构化，analyze prompt 增加"parentText 即完整自述，目标/担心/背景内联其中请识别使用"。docker 实测 saferVersion 用上了内联目标。
- **④ 三专项写回不一致**：教育/预演的 fire-and-forget `ingestEpisode` 改为 `enqueueJob('episode_ingest')`（deriveEpisodeId 幂等键，对齐 daily）；家庭规划补 episode_ingest（原先只写 dailyUpdate、不写 Episode）。docker 实测：episode_ingest 5 job 全 succeeded、5 Episode 全带向量（含家庭规划输入）、可追踪可重试。
- **⑤ 教育诊断 ready 但内容空→空结果卡**：modeReading+keyTensions 都空时降级 uiMode 为 light_followup，不返回空 result_view。
- **⑥ record-child 过时文案**：实测其走 `episode_ingest` 持久化，"MVP 演示版不写库"文案误导，已校正为"进入长期记忆"。
- **遗留（独立一轮）**：~~mock/localStorage 大范围残留~~ → **核心采集闭环已完成，见 功能-5**。剩余独立流：~~multi-view/child-voice~~ → **已完成，见 功能-6**；~~weekly-review 契约~~ → **已完成，见 功能-7**；~~profileSnapshot 多端持久化~~ → **已完成，见 功能-8**。

### ✅ 功能-5 首次建模采集闭环去 mock + 打通 EntryEvidencePack→FamilyModel→Brief→Board 🔴（已完成 · docker 验证）
> 五入口采集流此前「半真半假」：generating 已真调 synthesis+diagnosis，但 final-follow-up 纯 mock 且丢答案、五入口 summary/follow-up 用 mock 作初值兜底、EntryEvidencePack(L3) 从不落库。本轮接真闭环。
- **L3 采集即写**：抽 `src/lib/server/memory/entry-builder.ts`（buildEntryPack/classifyFacts/LEGACY_TO_NEW 共享）；`/api/entry/analyze` stage=summary 时构造 EntryEvidencePack + `enqueueJob('memory_write')` 持久化。**修复 L3 从不落库**（synthesis 的 memory_write 只写 network/hypotheses，不写 pack）。docker 实测 `entry_evidence_packs` 采集后即有该入口包（learning_homework + 真 summary）。
- **final-follow-up 接真 + 答案不丢**：页面 useEffect 调 `/api/entry/analyze`(entryType=final，聚合五入口 stageSummaries) 取真追问替换 `mockFinalFollowUp`，loading/重试；`handleSubmit` `createFollowUpRecord(entryType=final)` 持久化（不再 setTimeout 丢弃）；generating 读 final 答案作 `crossCuttingSupplement` 传 `/api/synthesis`，pipeline 并入综合 prompt。docker 实测 final 返回真问题（非固定 mock）、synthesis 带补充不报错（5 机制 6 跨入口）。
- **10 采集页去 mock 改 loading**：5 summary + 5 follow-up 去掉 `getMockStageSummary`/`getMockFollowUp` 初值，load() 提到组件作用域支持真重试，成功才渲染、失败/无 key 显示重试（不再假数据）；`mockOutputs.ts` 删死导出（10 per-entry + 2 getMock + mockFinalFollowUp）。
- **验证**：typecheck+build 通过（10 页体积下降）；docker C 实测 entry/analyze 写 L3、final 真问题、synthesis 接补充、job 全 succeeded、闭环到 board_snapshots/family_memory_digests。
- **本轮不含**：multi-view+child-voice（独立原型流）、weekly-review GET/POST 契约、profileSnapshot localStorage→DB 多端持久化。conflict/observation/weekly 的 mock fallback 保留。

### ✅ 功能-6 多视角校正 multi-view + child-voice 接真 🟡（已完成 · docker 验证）
> child-voice→multi-view 是唯一还整条 mock + 丢弃用户输入的孤立流程（child-voice 无人链接、孩子原话被丢，multi-view 显示静态 mockMultiViewCorrection）。本轮收掉。
- **三视角真生成**：新 prompt `prompts/front/multiViewCorrection.md`（key=multiViewCorrection）+ API `app/api/multi-view/route.ts`——家长视角（`buildDailyDialogueRetrievalPacket` 画像/机制）、孩子视角（孩子原话 + 本次输入）、老师视角（`getEntryEvidencePack('relationship_environment')` 学校/老师事实）；LLM 输出 headline/summary/三视角/finalChips。
- **老师视角红线**：teacherView 只基于环境入口真事实，**没有就诚实写"学校那边目前还不清楚"，绝不编造**。docker 实测：无环境数据→诚实不清楚；建环境入口后→用真老师观察（课堂能跟上、较少举手、被动配合）。
- **孩子原话不丢**：child-voice 提交 sessionStorage 暂存→multi-view 即时用；multi-view API 把孩子原话写回（episode_ingest + `[孩子视角]` dailyUpdate，分类 new_supporting_evidence）。
- **可达性**：profile/result 新增「听听孩子自己怎么说」入口（child-voice 此前无人链接）；multi-view 返回 profile/result 闭环。删 `mockMultiViewCorrection`。
- **验证**：typecheck+build 通过；docker C 实测三视角真生成、teacherView 诚实降级 vs 真数据两路径、孩子原话写回 `[孩子视角]`×2 + Episode、响应无后台字段泄露。

### ✅ 功能-7 周报 weekly-review GET/POST 契约修复 🔴（已完成 · docker 验证）
> 前端 `/weekly-report` 一直 **POST** `/api/profile/weekly-review {observations}` 期望 AI 周报 `{headline/summary/repeatedPatterns/keyObservation/nextWatchPoints}`，但后端**只有 GET**（返 sessionCount/recentClues 统计）。**缺 POST handler → 前端 POST 405 → catch → 周报永远显示 mockWeeklyReport。**
- **补 POST handler**：`app/api/profile/weekly-review/route.ts` 加 POST——接 `{observations}` → 新 prompt `prompts/front/weeklyReview.md`（key=weeklyReview，平和不评判、headline 是倾向非"更懒了"标签）→ 返回前端期望的 5 字段；无 key/失败返 503，**不出假数据**。GET 保留不动（api-client 仍有消费方、查的旧表存在）。
- **前端去 mock**：`weekly-report/page.tsx` 去 `mockWeeklyReport` 初值，改 loading/error 重试；删 `mockWeeklyReport` 死导出。
- **验证**：typecheck+build 通过；docker C 实测 POST 真观察→真周报（headline「一遇到可能又被加任务就先缩回去」+ 4 模式 + keyObservation + 3 watchPoints）、空观察→400 NO_OBSERVATIONS。

### ✅ 功能-8 孩子画像跨设备/重装持久化 🟠（已完成 · docker 验证）
> `/profile/result` 及 evidence/verify/deep 四页都只读 localStorage 的 `LocalProfileSnapshot`，新设备/重装后 localStorage 空 → 画像不显示（虽然 ChildStructureModel 真模型在 DB）。本轮让画像快照持久化到 DB、跨设备可取回。
- **DB 持久化**：`database-manager` 加 `BuiltProfileSnapshot` 类型 + `saveBuiltProfileSnapshot`/`getLatestBuiltProfileSnapshot`（复用 memory_layer_items 一层 `built_profile_snapshots`，`f:c:latest` 单例、按租户隔离，零迁移）；新 API `app/api/profile/built/route.ts` GET+POST。
- **写入**：generating 生成 localStorage 快照后同步 POST 到 DB（异步不阻塞）。
- **跨设备读回**：home 开屏检查——本机有画像直接用；本机无则 GET DB → `hydrateProfileFromRemote` 回灌 localStorage（之后全应用 `hasProfile()`/`getLatestProfile()` 同步生效）；profile/result 本地空时也 DB-first 兜底回灌。home「建立孩子画像」卡在已有画像时变「孩子画像·去查看」→ /profile/result（补发现路径）。
- **验证**：typecheck+build 通过；docker C 模拟跨设备——设备A POST 写入→设备B（localStorage 空）GET 完整取回（completeness/coreJudgment/evidence/verificationPoints）；psql `built_profile_snapshots` = `f_demo:c_demo:latest` 单例。
- **本轮不含**：conflict/observation 的 mock fallback（合理降级保留）。

### ✅ 工程-1 构建/部署/契约/无障碍 4 项工程修复 🟢（已完成 · 实测）
> 真实运行发现的工程问题。
- **① Next lockfile patch 报错**（`Cannot read properties of undefined (reading 'os')`）：根因是 **Next 14.2.35 自身打包版本偏移**——patcher 的 `fetchPkgInfo` 按 next 版本(14.2.35)查 swc 包，但 @next/swc-* 只发到 14.2.33（next 14.2.35 的 swc 依赖也指向 14.2.33），查到 undefined 崩。我们 lockfile 本身正确。修复：移除 package.json 里错误的手动 `optionalDependencies @next/swc-darwin-arm64:14.2.35`（pin 了不存在的 swc 版本）+ build/dev 脚本加官方逃生口 `NEXT_IGNORE_INCORRECT_LOCKFILE=1`。实测 build/dev 不再报错。
- **② output:standalone 与部署不一致**：部署用 `next start`（pm2 fork，见 `ecosystem.config.js`），与 `output:'standalone'` 不兼容（`next start does not work with standalone`）。移除 `next.config.mjs` 的 `output:'standalone'`，对齐 `next start`。
- **③ /api/entry/analyze 无 key 返回 ok:true 但 data 空**：契约不清。改为无 AI 结果时返 **503 ok:false**（`ENTRY_SUMMARY_UNAVAILABLE`/`ENTRY_FOLLOWUP_UNAVAILABLE`）；前端 summary/follow-up/final 页本已 `if(ok&&data?.x)else setError`，完全兼容。实测无 key→503。
- **④ favicon 404 + 登录 password 不在 form 内**：新增 `app/icon.svg`（品牌 eye 图标）+ middleware matcher 排除 `icon.svg`（原被登录拦截 307）→ HTML 含 `<link rel=icon>`、`/icon.svg` 200；登录页 `auth-card` 改 `<form onSubmit>`、PrimaryButton `type=submit`、加 `autoComplete=username/current-password`，消除浏览器/无障碍告警。

### ✅ 工程-2 测评反馈「前台快后台深」P0 三项 🟠（已完成 · docker 验证）
> 依《测评反馈1》总纲做 7 路 agent 8 项审计；前台已基本快（daily/专项 uiMode+异步写回），P0 先收红线+体验破裂点（P1 entry-evidence 后台化、P2 后台深度循环留独立专题）。
- **① family-planner 泄露后台字段**：light_followup 分支把后台决策字段 `enoughToPlan` 返回前台（违反"前台不背后台字段"红线）。从响应 plan 移除（uiMode 已表达，`insufficient` 仅内部供 decideFeatureUI）。docker 实测响应不含 enoughToPlan。
- **② board 刚建模空态（竞态）**：`digest_update` 异步入队、前端一次性 fetch → 读到 FALLBACK 空态。后端无快照时返 `pending:true`；前端薄内容时保持 loading + 每 2.5s 轮询重试（≤5 次，digest 写好快照即命中）+ 展示 `updatedAt`。docker 实测无快照→pending:true。
- **③ diagnosis/synthesis 空字段**：根因 **generating 调 diagnosis 时没传 `synthesisOutput`** → 诊断拿不到机制矩阵/跨入口证据/handoff → 结构字段薄。修：generating 传 `synthesisOutput: syn`；diagnosis `familyInteractionLoop` 过滤空 aiLoops 用兜底（原 LLM 返空条目直接用导致 patternName/steps 空）；synthesis 过滤核心字段全空的跨入口条目并 WARN；薄输入记 WARN 暴露不静默。docker 实测带 synthesisOutput 后 lowMisjudgmentFacts 13/mainMechanismCandidates 5/familyInteractionLoop 非空(5 步)。
- **待续**：P1 entry/analyze 前台只返阶段反馈、EntryEvidencePack 下沉独立后台 evidence agent；P2 后台 Evidence Agent(Claim/Evidence/Rebuttal+confidence)、FamilyModel 异步循环、闭环可观测(job-status/evidenceRefs)。

### ✅ 工程-3 P1 entry-evidence 后台化（前台快后台深·核心一刀）🟠（已完成 · docker 验证）
> 测评反馈核心：前台只输出阶段反馈，EntryEvidencePack 由专门后台 evidence agent 深度生成，不再"前台 summary 顺手写规则包"。
- **后台证据建造 Agent**：新 prompt `prompts/background/entryEvidenceBuilder.md`（key=entryEvidenceBuilder，深度拆解 decomposedInput 分层：childBehaviors/parentEvaluations/parentActions/triggerPoints/childQuotes/parentGoals + candidateMechanisms 带 evidenceStrength + handoff + confidence；底线事实≠评价）+ 新 pipeline `src/lib/server/memory/entry-evidence/builder.ts`（`runEntryEvidenceBuild` LLM 深拆→组装 EntryEvidencePack→executeWritePlan；LLM 失败/thin 回退规则版 `buildEntryPack` 保证 L3 不退化）+ 新 job type `entry_evidence`（queue.ts，不吞异常驱动重试）。
- **entry/analyze 拆分**：stage=summary 把内联规则 `buildEntryPack`+memory_write 换成 `enqueueJob('entry_evidence',{rawText,frontSummary,facts,...})`（idem=null 允许重提交重建，pack 按 entryName upsert 幂等）。**前台只返 {mainJudgment,facts,note}，证据包不进响应**。
- **验证**：typecheck+build 通过；docker C：前台响应只含阶段反馈（无 decomposedInput）；entry_evidence job succeeded；深拆包 `childBehaviors`（客观行为）与 `parentEvaluations`（"拖/烦/老盯着"标为家长评价）正确分离、`parentGoals`/`triggerPoints` 齐全——远细于规则版关键词分类；全程异步（前台秒回，深拆 ~20s 后台完成）；规则版回退路径亦通。
- **待续 P2**：Evidence Agent 升 Claim/Evidence/Rebuttal 三层 + confidence/coverage 矩阵持久化；FamilyModel 异步循环（诊断不一次升 stable、定期重评假设）；闭环可观测（job-status/evidenceRefs 全链路）。

### ✅ 工程-4 P2-① 后台「假设复核 Agent」（FamilyModel 异步循环起点）🟠（已完成 · docker 验证）
> 测评反馈 P2 核心：后台不能只是"规则数据搬运"，要有真正的反证收集 + 置信度评估 + 不一次升 stable。本轮做第一步——拿已有待验证假设对照近期证据复核。
- **复核 Agent**：新 prompt `prompts/background/modelReview.md`（key=modelReview，复核已有 pending hypotheses，对照 recentEpisodes/highValueAtoms 找支持与反证→给 weight/status/counterEvidence，谨慎不下稳定结论、反证不编造）+ pipeline `src/lib/server/memory/model-review/reviewer.ts`（`runModelReview`：读活跃假设→retrieveContextPack 取证据(含 counter_evidence atom)→LLM 复核→更新 weight/possibleCounterEvidence/status→savePendingHypotheses；LLM 不可用则不改动）。
- **链式触发**：`model_review` job（queue.ts），由 `memory_write` 在写了假设时（`plan.pendingHypothesesToCreateOrUpdate>0`）链式触发——保证假设已落库再复核，且不在 daily 等普通写入上空跑 LLM。
- **谨慎语义**：本步只允许 status pending/supported/weakened，**绝不升 resolved（稳定结论）**；有反证则削弱 + 降 weight。
- **验证**：typecheck+build 通过；docker C 实测——插两条假设 + 触发复核：hyp「被催与拖延绑定、自主时能启动」→ weight medium→high、status→**supported**；hyp「孩子就是不自觉要一直盯」→ weight medium→low、status→**weakened**、**收集到反证「周末主动订正了没人催」**。后台对"不自觉"标签主动反证削弱，模型越用越准。
- **✅ P2-② 定期重评循环（已完成）**：daily 流新证据落库后按 10 分钟时间桶（`modelReviewBucketKey` 幂等限流）触发 `model_review`——不再只在建模时复核，而是随日常使用持续重评假设；无活跃假设时 `runModelReview` 提前 no-op 不空跑 LLM。docker 实测：发一条带新证据（自主完成作业）的 daily → model_review succeeded → hyp「自主时能启动」low→high/supported(+2 支持)、hyp「孩子不自觉」medium→low/weakened(+2 反证)。FamilyModel 异步循环跑通。
- **待续 P2 余项**：~~闭环可观测 job-status/evidenceRefs~~ → **已完成，见 工程-6**；Evidence Agent 升 Claim/Evidence/Rebuttal 三层 → **评估后不做（与 P2-① 反证/置信度重叠，过度建模），见 工程-6 末**。

### ✅ 工程-5 隔离/健壮性 3 项修复（审计）🔴🟠（已完成 · docker 验证 #3）
> 真实运行审计发现的隔离与健壮性问题。
- **🔴 登录态与本地画像缓存未隔离**：logout 只删 cookie，不清 `childos.v1`；profileStorage 固定写 DEFAULT 租户 → 同浏览器切账号会看到上一位用户的画像/状态。修：`localStorageService` 加 `clearAllChildOSData`（清所有 `childos` 前缀 local+session）；home `logout()` 调用 + 跳 `/login`；login/register/demo 成功进入前也清（防切账号串）。DB 按租户隔离是真源，清本地缓存无损。
- **🟠 profile/generating 异常静默跳结果页**：catch 直接 `router.push('/profile/result')` → 展示旧画像/空态、无失败原因。修：catch 改 `setError` + 重试按钮（重跑 useEffect）+ 返回建模入口，不再静默跳转。
- **🟠 model_review 对旧数据不稳**：`runModelReview` 直接展开 `h.possibleCounterEvidence`/`h.supportingEvidence`，旧版/外部记录缺字段会抛错→job 重试死循环。修：展开前统一 `Array.isArray(...)?...:[]` 兜底，weight/status 缺也兜底。docker 实测：插缺字段的旧格式假设触发 model_review → job succeeded（attempts=1 无错）、字段自动补齐。

### ✅ 工程-6 P2-④ 闭环可观测（生成→写入→检索→使用 可验证）🟠（已完成 · docker 验证）
> 测评反馈反复强调："检查 job 是否真执行、失败前台无感知、evidenceRefs 全链路标注"。本轮把后台闭环做可观测。
- **A · job-status 端点**：`queue.ts` 加 `getJobHealth(tenant)`（按 `COALESCE(payload->tenant/ctx ->>familyId)` 查 job_queue 分组计数 + 近期失败）+ 新 `app/api/jobs/status/route.ts`。docker 实测返回 byType（memory_write/digest_update/episode_ingest/entry_evidence/model_review 各状态）+ totals(pending/running/succeeded/failed) + recentFailures——后台链路各环可见、失败可查。
- **B · board 展示证据来源**：digest/updaters 早已把带 snippet 的 evidenceRefs 写进 board_snapshots，但前端没展示。board 页加「这些判断的依据」折叠区渲染 snippet——看板结论可追溯到具体证据。docker 实测 digest 完成后 board 返回 evidenceRefs（version=1、2 条带 snippet），psql 确认 board_snapshots.evidence_refs 落库。
- **P2-③（Claim/Evidence/Rebuttal 三层 + coverage 矩阵）评估后不做**：P2-①② 的假设复核已落地"支持/反证(rebuttal)/置信度"实质，再做一层正式三层结构是大改且边际价值低（过度建模）。P2 至此（①②④）已把反馈"后台深"核心（深拆/反证/置信度/异步循环/可观测）全部落地。

- **暂缓（需产品决策）**：~~教育模式诊断独立入口（5.3）~~ → **已完成，见 功能-3**；~~预演/规划接入共用 FeatureUIRouter~~ → **已完成，见 功能-4**。


### ✅ 架构-4 后台任务队列与重试 🟠（已完成 · docker 8 场景验证通过）
- **已做**：新增 `src/lib/server/jobs/queue.ts` —— PG `job_queue` 表（pending/running/succeeded/failed/retrying 五状态，文档 14.3）+ in-process poller（pm2 单实例，`instrumentation.ts` 启动 + health/readiness 兜底，dev 默认关）。7 个 fire-and-forget 落点改 `enqueueJob`（daily/daily-stream/diagnosis/synthesis 的 memory_write + daily-stream/record-child/entry 的 episode_ingest；memory/write 保持同步）。
- **对抗审查修的 8 个 P0**：① `claimAndRunOne` 用 `FOR UPDATE SKIP LOCKED` + 终态 `AND status='running'` CAS 守卫；② 心跳每 30s 推 `updated_at` 防长任务被僵尸回收误判；③ 僵尸回收排除本进程 inflight；④ episode 重试幂等——`deriveEpisodeId`（tenant+sha(text)）+ atomId 下标派生 + DELETE-then-insert；⑤ `executeWritePlan` 唯一非幂等点 `indexId` 改确定派生；⑥ `ingestEpisodeStrict` 不吞异常、`EPISODE_VECTOR_UNAVAILABLE` fail-fast；⑦ 独立连接池（max=2）不饿死前台；⑧ DB 未启用→inline 降级、入队失败→inline 回退。
- **验证（docker pgvector + 真实 LLM）**：① 状态机流转 succeeded；② 同文本 3 次→**仅 1 Episode**（idempotency_key 去重）；③ atom 无重复/孤儿；④ 僵尸回收（stale running→retrying→重跑 succeeded，重跑幂等仍 1 Episode）；⑤ 10 并发→0 重复入队→1 Episode；⑥ 失败 job→retrying（退避+last_error）→耗尽 max_attempts→failed。tsc+build 通过。
- **遗留**：DB 未启用的 inline 降级路径为代码验证（未运行时测）；poller 仅 pm2 单实例假设，多实例需加分布式锁。

---

## 第四批：工程质量隐患

### ✅ 质量-1 多租户隔离 🔴（已完成 · docker 双用户隔离验证通过）
- **问题**：记忆域（10 层 memory + Episode + 检索 + decision-engine + context）全写死 demo 租户，完全不按 familyId/childId 隔离；且身份域 `f_demo/c_demo` 与记忆域 `family_demo/child_demo` 两套 demo 值分裂。违反交付文档 14.2。
- **已做（显式传参主干）**：新增 `memory/tenant.ts`（`TenantId`/`DEMO_TENANT`/`resolveTenant`）；database-manager ~28 个 save/get 函数 + decision-engine/index-tag-engine/router/pipeline/orchestration 全部把 `tenant: TenantId` 设为**必填参数**（漏传即 tsc 编译失败，编译期防跨租户泄漏）；8 个 route 入口 `resolveTenant()` 注入并向下透传，后台 fire-and-forget 在 resolve 后按值捕获租户。
- **修复两个 P0**：① `memory_layer_items` PK 改为 `(family_id, child_id, layer_name, item_id)` + `entry_evidence_packs` itemId 加租户前缀（原裸 `entry:xxx` 会跨租户撞 PK 互相覆盖）；② 进程内存 store 按 `${familyId}:${childId}` 分桶（原单 key 跨租户串数据）。
- **demo 值统一**：全部统一为 `f_demo/c_demo`（含两份设计都漏的 `storageSeed.ts`）；附幂等迁移 SQL（迁存量 demo 值 + entry itemId 补前缀 + 改 PK，`EXCEPTION` 包裹不阻断启动）。
- **验证**：tsc + build 通过；docker pgvector 注册两个真实用户 A/B（各独立 familyId），各发 daily → Episode/Atom/memory_layer_items 按 family_id 隔离、**A 检索域看不到 B**；PK 碰撞回归：两租户同名 `learning_homework` entry pack 共存不覆盖。

### ✅ 质量-2 内存降级显式告警 🟢（已完成）
- **已做**：`database-manager.ts` 的 `getServerMemoryStore()` 在首次降级到进程内存时显式 `console.warn` 一次（交付文档 10.4），提示「数据未持久化、重启丢失」，不再静默。
- **遗留**：DB 写入失败本身由上层 `try/catch` 抛出并记录（非静默落内存），符合预期；「写库失败即硬报错」开关可作为后续可选项。

### ✅ 质量-3 过时测试脚本 🟢（已完成 · 归档）
- **已做**：12 个过时脚本 `git mv` 到 `scripts/legacy/`（保留 git 历史）+ 加 `scripts/legacy/README.md` 说明：8 个 `test_*.py`（旧 problem/深度复盘流程，含硬编码服务器路径）+ 4 个 xiaoyin 系列（`test-xiaoyin-v2/v3.mjs`、`xiaoyin-test-data.{mjs,ts}`）。
- **保留活跃**：`build-prompts.mjs`（npm 钩子，勿删）、`verify-pgvector.mjs`、`test-vector-retrieval.mjs`、`test-input-strategy.mjs`、`test-profile-rehearsal.mjs`（仍调当前端点）。

### ✅ 质量-4 diagnosis / synthesis 写入异步化 🟠（已完成 · 含误判修正）
- **核查修正**：之前判断「diagnosis/synthesis 返回完整内部对象违反 6.5」**有误**。核查消费方 `app/profile/generating/page.tsx` 后确认：这两个 route **不是前台对话 route，而是画像生成管线的数据步骤**——前端深度消费其完整字段（`candidateMechanismMatrix`、`primaryMechanismChain`、`secondMeConditionalProfile` 等）在客户端组装孩子画像。其完整输出**正确且必要**，不应收敛。
- **已做**：将 `await runMemoryWritePipeline` 改为异步（交付文档 6.3），并去除前端从未使用的 `memoryWrite` 响应字段。画像生成返回更快、写入不阻塞，且统一了全部 route 的写入模式。
- **遗留观察**：画像在前端用内部机制字段组装（`generating/page.tsx`）属现有架构特征；若要彻底做到「前台不接触内部判断」，需把组装逻辑移到服务端，成本较大，列入长期项。

### ✅ 体验-1 全输入语音化 🟢（已完成）
- **背景**：手机端打字不便，要求「所有输入都能语音」。盘点发现项目已有**真实 ASR**（腾讯云实时识别，`useTencentAsrInput` → WS `/api/asr/stream`），已接入 BottomVoiceBar/VoiceOverlay/首页/record-child(仅 eventText)；**缺口在覆盖率，不在能力**——不换引擎，只铺开复用。
- **已做**：新建零配置行内组件 `VoiceFieldButton`（复用唯一抓手 `useTencentAsrInput`；模块级**全局单活麦克风**防多字段并发；连不上 ASR 显式红字降级、绝不静默）+ `appendTranscript` 追加助手。铺进 12 个原无语音输入面：
  - 共享组件 `SpecialCollectionView` + `LightFollowupView`（一次惠及 教育诊断/家庭规划/沟通预演 **3 页×2**）
  - `record-child` 三字段各自语音（替换原仅回填 eventText 的页面级单语音）
  - 画像 `profile/build/{study,routine,communication,emotion,environment}/follow-up` 五同构页 + `final-follow-up`
  - `child-voice` 孩子原话；`ArchiveCard` 可编辑字段（除日期）
- **明确不加**：登录页手机号/密码（敏感字段）。
- **基建约束**：`/api/asr/stream` 不在 Next route，需经 `server-ws.js`（standalone）或 nginx+`asr-proxy.js`(:3001) 提供；`next start`（含 Docker 档位 C）默认不带该 handler，故 Docker 只能验「按钮渲染 + 连不上优雅降级」，真实转写须在已接 ASR 端点的环境验。
- **验证**：tsc + build 通过；Docker 重建 + demo session 实测 education-diagnosis / family-planner / record-child(×3) / emotion-follow-up / child-voice 均 SSR 渲染麦克风按钮。

### ✅ 审计-3 P0/P1/P2 逐条核验与修复 🟢（已完成）
- **方法**：10 路并行核验工作流逐条对照真实代码（不盲信审计结论）。**3 条不实/设计如此**：P0-1 readiness `ready:false` 是没配 `.env.local` 的预期输出（非 bug）；P2 首次建模异步交付链是有意解耦（board 页已有轮询+pending+实时回退兜底）；P1P2 日常深度复盘已在异步 synthesis/model-review、"不上前台"是 P0 刻意设计。
- **已修 6 项（小、安全、高价值）**：
  - **profile/snapshot（P0/P1）**：补 `verifyAppApi` 守卫；租户改以会话身份为准、忽略 query `familyId/childId`（消除匿名跨租户 IDOR）；去 `waitMock`；无真实 digest 且 LLM 失败时返 **503** 不再返回硬编码假画像（"数学作业开始前更容易拖延"…）。
  - **/api/board（P0-2b）**：此前 GET 完全无鉴权门，补 `verifyAppApi`。
  - **verifyInternalApi（P2）**：设了内部 token 时**只认 token**，不再因可伪造的同源 Referer/Origin(`.includes` 子串)放行；未设 token 时保留 dev/同源(改 `isSameOriginRequest` 精确比较)兜底。
  - **family-profile（P1）**：删初始假线索 `recentItems`→空+空态；`currentFocus` 改温和空态；去"演示看板/真实数据库接入后""只保存 mock 数据"文案。不动导航与 /board。
  - **record-child（P1）**：route 删 `waitMock(260)`；page 统一保存文案为"会进入长期记忆"，去"本地演示记录"过时表述。
  - **observation/conflict-result/rehearsal-result（P2）**：失败/空流不再回退 mock 当真结果，改为显式错误态+重试。
  - **P1P2 日常深拆字段（核实为误判 → 删死字段）**：审计称 orchestration `decomposedInput` 关键字段恒空。3 路并行追踪证实那是**死字段** `OrchestrationOutput.decomposedInput`（database.ts，11 字段）——构造后零消费、不写库、不喂 synthesis；审计把它和**同名但不同结构**的 `EntryEvidencePack.decomposedInput`（13 字段，首字段 verifiableFacts，synthesis 真正消费、已由 entry-builder/LLM 真实填充）搞混了。**填它毫无意义**，故删除死字段 + 2 个只为它服务的死函数(extractFacts/extractChildBehaviors) + 连带 hasLabel/import，消除引发误判的同名冲突。真正的深拆已在异步 EntryEvidencePack→synthesis 链路由 LLM 完成。
- **未做（留待）**：无。本轮 10 条审计全部闭环（修复 / 判为不实或设计如此 / 删死字段）。
- **验证**：tsc + build 通过；prod server.js 实测：匿名 snapshot/board/IDOR-query 全 401；登录后 snapshot 503 且无假画像文案；jobs/status 设 token 后同源伪造 401、正确 Bearer 200、错误 401；**P0-2b：无 cookie 401、伪造 cookie+同源 401（修复前可过门）、真实演示会话 503**。

### ✅ 审计-4 第二批 P0/P1/P2 核验与修复 🟢（安全批已完成）
- **方法**：5 路并行核验 + 全 API 路由鉴权普查（34 个 route.ts 逐个查 PUBLIC_PATHS / route 级守卫）。
- **已修（安全批，运行时验证）**：
  - **11 条缺鉴权路由补 verifyAppApi**：普查发现 record-child / advice-generate / archive-confirm / archive-draft / problem-answer / problem-answer-stream / understanding-feedback / understanding-generate / conversations-start / conversations-[id]-state / **asr-token** 既不在 PUBLIC_PATHS 又无 route 级守卫，仅靠 middleware 查 cookie 存在性 → 伪造 `childos_session` 即可访问（asr-token 还会泄露腾讯 ASR 凭证）。统一补 `if (!(await verifyAppApi(request))) return authError()`；含 3 处签名调整（asr-token 加 request 形参、conversations-[id]-state 把 `_` 改 request、流式守卫前置）。
  - **jobs/status verifyInternalApi 回归修复**：上轮收紧时误把「no-token prod」从拒绝改成了「同源放行」（同源头可伪造）。改回「dev 直通、prod 一律拒绝」。唯一调用方 jobs/status，无前端依赖。
  - **验证**：tsc+build 通过；prod server.js 实测伪造 cookie 在 5 类路由全 401、jobs/status 伪造 Referer 401、真实演示会话 record-child 200。
  - **record-child 联动 Board + 堵 body-id IDOR（增量）**：record-child 此前不入队 memory_write，记录孩子**不驱动 Board/Brief**。已加 `enqueueJob('memory_write', {plan})`（buildMemoryWritePlan+createDailyUpdate，与 daily 对齐 → digest_update → BoardSnapshot/Brief），旧同步写保留兼容。并把 `getRequestIdentity({body ids})` 换成 `resolveTenant()`（会话身份），顺带堵掉 body 越权写他人租户。prod 实测演示会话 200、伪造 cookie+body 越权 401。
- **核实为不实/设计如此（未改）**：P1 readiness（没配 .env.local，判定逻辑正确）；P1/P2 FamilyModel v1 强保证（有意异步解耦，board 页 pending+轮询兜底完整）。
- **真但留待（需产品决策 / feature 级）**：
  - **日常对话深拆做进主链路（Layer 1 + Layer 2 已做）**：Layer 1——① 修 linkedAreas 恒空 bug：daily 两路此前对 `string[]` 取 `.entryName` 永远空，改 `deriveLinkedAreas(text)`（家长友好主题标签，零 LLM、不暴露内部机制名）。② Board 加「判断变化/阶段复盘」分区：BoardCore+boardUpdater prompt 加 judgmentChanges（区别于 recentChanges），board 路由类型/FALLBACK/normalize + 页面渲染。Layer 2——新增 `daily_deep` 异步 job + `dailyDecompose` agent：每轮日常对话后台六维深拆（事实/孩子原话/家长动作/触发点/情绪/目标），**保守**生成 0-2 条待验证假设（去重不重置 model_review 已更新 status），经 memory_write → 链式 model_review 完成「反馈复盘」→ 经 brief/pendingHypotheses 进 Board judgmentChanges。`runDailyDeep` 返回 plan 由 queue 入队（无循环依赖）；无 key/LLM 失败/无新机制 → no-op，daily 秒回不退化。prod 实测 daily 200、inline no-op 不炸；全链 LLM→假设→model_review→Board 需 DB+LLM 环境验证（架构与 entry-evidence/synthesis 一致）。
  - **底栏导航对齐文档**（真，需产品定基准）：共享 BottomNavTabs 4 项 + 首页内联 6 项，都≠文档建议的 5 项（对话/预演/诊断/记录/看板）；看板入口在首页是大卡片不在底栏。
  - **材料理解入口**（真，net-new feature）：uploaded_material/material_observation 是被动分类枚举，无上传 UI/API/对象存储；需整套新建。
- **附加 P1（部分已处理）**：`getRequestIdentity` 无会话时回退 body 的 familyId/childId 的 body-id IDOR——**record-child 与 conversations-start 均已改 `resolveTenant()`（会话身份）堵上**。剩余：conversation 系列 store 函数仅按 conversationId 操作、无租户隔离（archive/confirm、problem/answer、conversations/[id]/state 等已补 verifyAppApi 但 store 层未校验租户）。建议后续在 store 层加租户校验。

---

## 验收自检清单（每次改动后跑，依据文档 10.3）

- [ ] `npm run typecheck` 通过
- [ ] `npm run build` 通过
- [ ] `/api/daily` 只返回 `visibleReply + traceId`（+ 可见 linkedAreas），不泄露内部字段
- [ ] 后台写入不阻塞前台回复
- [ ] 追问文案包含「在区分什么」（文档 4.4）
- [ ] 前台无审判感、不泛泛建议、不标签化、不医疗化（文档 11.2）

---

*最后更新：2026-06-18 ｜ 已完成：第一批（P0-1/P0-2）、第二批（P0-3/family_planner/board）、质量-1 多租户隔离、质量-2/质量-4、架构-1 三层语义检索、架构-4 job_queue（docker 8 场景验证通过）、体验-1 全输入语音化、审计-3 P0/P1/P2 逐条核验与修复。均通过 typecheck + build。*
