---
description: 语音/录音排障铁律——先查代码，禁止甩外部
alwaysApply: true
---

# 语音排障：默认是代码问题（用户铁律）

完整复盘与传承经验（必读）：
[`.agents/postmortems/2026-07-14-voice-asr-outage.md`](../../.agents/postmortems/2026-07-14-voice-asr-outage.md)

用户明确要求记住。排障实时 ASR、亲子录音、按住说话时必须遵守。

## 默认认定

1. **问题在代码，不在用户侧、不在运营商、不在「讯飞突然封微信出口」、不在「微信平台突然拦截」。**
2. 「昨天整段能用、今天下午起整段不能用」优先对照：**本会话/今日改过的代码、部署、小程序 dist、连接写法回归**，不要用「外部网络层极低概率突变」当主结论。
3. 若同一故障在 **直连讯飞 WSS** 与 **本站代理 WSS** 上表现相同（如都 `Connection refused`），而同机 **HTTPS 到本站正常**，则排除「讯飞封 IP」；继续查 **客户端 `connectSocket` 是否双开、签名是否被用两次、监听注册时序、并发 socket 泄漏、录音 claim 争用**。
4. 用户纠正思考方向后：**禁止换皮外部假说**；下一句必须是代码 diff / 日志指纹 / 对照实验。

## connectSocket 硬约束（本会话已踩坑）

- 讯飞签名 URL **单次有效**。`Taro.connectSocket` 在微信端返回 Promise，但**内部已经调用了一次** `wx.connectSocket`。
- **禁止**「先 Taro（得 Promise）→ 再 native 第二次 connect」：第二次会烧掉签名 / 泄漏无监听 SocketTask → 稳定 `Connection refused`，且 iOS 上 socketTaskId 堆高后整段 WSS 全拒。
- 正确：全链路 **只调用一次** connectSocket；拿到 SocketTask 后 **同一调用栈** 注册 onOpen/onError/onClose/onMessage。
- 若曾双开泄漏，需用户 **彻底杀掉微信进程再开**（清的是我们泄漏的客户端 socket 状态，不是甩锅用户环境）。
- **刚扫码正常、几小时前开发预览必挂**（真机调试已断开）：最强旁证 = **微信进程内状态腐烂**；断调试 ≠ 清 socket/recorder。排障必须先问：扫码时间、是否杀过微信、正式/体验 vs 开发预览。

## 亲子录音时好时坏（已有代码线索，勿甩网络）

- UI「上传失败，请检查网络」且 **Network 无 URL** = `uploadFile` 在客户端预检失败，或 catch 吞了微信 `{ errMsg }`（不是 Error）。
- 与实时 ASR **共享** `recorderState` / 全局 `RecorderManager` / claim：`waitForRecorderIdle` 在 `active` 残留时会对空闲 recorder 误 `stop()`，可导致临时 mp3 失效 → 上传偶发失败；争用消失后「突然又能用」是代码竞态，不是玄学自愈。
- 排障必须透出微信原始 `errMsg`，禁止只显示泛化「网络失败」。

## 实时 ASR

- `/api/asr/iflytek/url` 200 只说明签名下发成功；真正失败看 Console `[asr-socket]`：`onOpen` / `onError` / `connectSocket success taskId=`。
- 真机调试 Network **经常看不到** WebSocket，且 Size 可能显示 0B——**不能**据此推断「没发出去」或「body 空」；以 Console 日志为准。
- **先拆链路**：实时 vs 亲子；**再**动手。排查改动会制造「医源性」第二现场，日志要能区分。

## 禁止

- 不要把「域名没配 / 没重新编译 / 真机调试搞的鬼」当主因反复甩给用户（另见用户 ASR 铁律）。
- 不要在未获「可以改语音」授权时改锁定语音文件；一旦授权，改完必须用 Console 证据验收，禁止用外部猜测结案。
- 决策前涉及微信 DevTools / connectSocket / errMsg 语义：**先检索官方文档与社区**，再下结论。
